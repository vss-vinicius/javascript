// Definindo uma variável 'v' que armazena uma função anônima que recebe um parâmetro 'x'
let v = function(x) {
    // Retornando o dobro do valor de 'x'
    return x * 2;
}

// Chamando a função armazenada na variável 'v' com o argumento 5 e exibindo o resultado no console
console.log(v(5));